<odoo>

    <record id="inherit_form_view_delivery_carrier_delivery_logistic" model="ir.ui.view">
        <field name="name">Herencia de vista Formulario de Método de envío para logística</field>
        <field name="model">delivery.carrier</field>
        <field name="inherit_id" ref="delivery.view_delivery_carrier_form"/>
        <field name="arch" type="xml">
            <field name="integration_level" position="after">
                <field name="route_id"/>
            </field>
        </field>
    </record>

</odoo>
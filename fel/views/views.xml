<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="fel.list">
      <field name="name">fel list</field>
      <field name="model">fel.fel</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="fel.action_window">
      <field name="name">fel window</field>
      <field name="res_model">fel.fel</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="fel.action_server">
      <field name="name">fel server</field>
      <field name="model_id" ref="model_fel_fel"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="fel" id="fel.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="fel.menu_1" parent="fel.menu_root"/>
    <menuitem name="Menu 2" id="fel.menu_2" parent="fel.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="fel.menu_1_list" parent="fel.menu_1"
              action="fel.action_window"/>
    <menuitem name="Server to list" id="fel" parent="fel.menu_2"
              action="fel.action_server"/>
    -->
  </data>
</odoo>